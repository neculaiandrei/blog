[
  '{{repeat(10, 15)}}',
  {
    title:  '{{lorem(3, "words")}}',
    content: '{{lorem(4, "paragraphs")}}',
    tags: [
      '{{repeat(5)}}',
      '{{lorem(1, "words")}}'
    ],
    isPublished: '{{bool()}}',
    dateCreated: '{{date(new Date(2016, 1, 1), new Date(2016, 5, 1))}}',
    datePublished: '{{date(new Date(2016, 6, 1), new Date())}}'
  }
]

// Run this in the database.
//db.getCollection('posts').find().forEach(function (doc) {
//   doc.dateCreated = new Date(doc.dateCreated);
//   doc.datePublished = new Date(doc.datePublished);
//   db.getCollection('posts').save(doc);
//});